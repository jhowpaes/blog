import {TextStyle} from 'react-native';

interface ITextStyles {
  input: TextStyle;
}

export const ITextStyle: ITextStyles = {
  input: {
    backgroundColor: '#fff',
    marginBottom: 15,
    borderRadius: 7,
  },
};
