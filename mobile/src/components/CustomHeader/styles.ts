import {ViewStyle} from 'react-native';

interface CustomHeaderStyles {
  holder: ViewStyle;
}

export const CustomHeaderStyle: CustomHeaderStyles = {
  holder: {
    height: 30,
    justifyContent: 'center',
    alignItems: 'center',
  },
};
