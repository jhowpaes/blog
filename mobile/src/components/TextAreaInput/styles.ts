import {TextStyle} from 'react-native';

interface TextAreaInputStyles {
  input: TextStyle;
}

export const TextAreaInputStyle: TextAreaInputStyles = {
  input: {
    height: 210,
    backgroundColor: '#fff',
    marginBottom: 15,
    borderRadius: 7,
  },
};
